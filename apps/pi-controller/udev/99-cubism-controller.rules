# Cubism: grant the `input` group read access to the 3-key macropad + knob
# so the pi-controller sidecar doesn't need root.
SUBSYSTEM=="input", ATTRS{idVendor}=="1189", ATTRS{idProduct}=="8890", MODE="0660", GROUP="input"
